# Join message
joinMessage: '{{player}} join the server.'
joinMessageJson: '{"text": "{{player}} join the server.", "color": "yellow"}'
# use json version or normal version.
useJsonVersion: true
# Monitor config file change/delete and reload/release config file.
fileWatchService: true